apiVersion: apps/v1
kind: Deployment
metadata:
  name: optuna
  namespace: evaluation
  labels:
    app: optuna
spec:
  replicas: 1
  selector:
    matchLabels:
      app: optuna
  template:
    metadata:
      labels:
        app: optuna
    spec:
      containers:
      - name: optuna
        image: gaocegege/optuna-tf-example:v1.0.0
        ports:
        - containerPort: 80